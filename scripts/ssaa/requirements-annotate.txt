# Minimal environment for SSAA-v3 annotation workers.
# This is NOT the full LAP training stack (no torch/jax/tensorflow/openpi) —
# only what the claim → extract → tools → audit → push path actually imports.
# Install into a fresh uv venv:  uv pip install -r scripts/ssaa/requirements-annotate.txt
numpy<2.0
h5py
pillow
opencv-python
pexpect          # one-time SSH ControlMaster password login
# ssaa_client.py and audit_v3.py are stdlib-only; system `ssh`/`rsync`/`scp`
# (OpenSSH) must also be installed. The viewer (view_droid_v3.py, human hinting
# only) additionally needs `gradio` and `matplotlib` — not required to annotate.
